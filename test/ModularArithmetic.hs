module ModularArithmetic where

import Lib (divisors, gcd', isRelativePrime, nPrimes, nthPrime, pow', relativePrimes, sqrt', totient, isPrime, isPowerOfSomeNumber)

elo :: IO ()
elo = do
  putStrLn "Find smallest natural number n, such that n^2 + n + 41 is"
  putStrLn " complex number"

  return ()

